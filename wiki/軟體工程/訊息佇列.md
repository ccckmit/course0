---
tags: [軟體工程, 訊息佇列, Kafka, RabbitMQ, 事件驅動]
date: 2026-04-15
source_count: 2
last_updated: 2026-04-15
---

# 訊息佇列：異步通信與事件驅動

## 概述

訊息佇列實現了服務之間的異步通信，提高了系統的解耦度和可擴展性。主要的訊息佇列系統包括 Apache Kafka（高吞吐量日誌系統）和 RabbitMQ（通用訊息代理）。

## Kafka 實現

```python
from confluent_kafka import Producer, Consumer, AdminClient
import json

class KafkaProducer:
    """Kafka 生產者"""
    
    def __init__(self, bootstrap_servers):
        self.producer = Producer({
            'bootstrap.servers': bootstrap_servers,
            'client.id': 'order-service'
        })
    
    def send_order_event(self, order_id, event_type, data):
        topic = 'order-events'
        message = {
            'order_id': order_id,
            'event_type': event_type,
            'data': data
        }
        
        self.producer.produce(
            topic,
            key=str(order_id),
            value=json.dumps(message).encode('utf-8'),
            callback=self.delivery_callback
        )
        self.producer.flush()
    
    @staticmethod
    def delivery_callback(err, msg):
        if err:
            print(f'Message delivery failed: {err}')
        else:
            print(f'Message delivered to {msg.topic()}')

class KafkaConsumer:
    """Kafka 消費者"""
    
    def __init__(self, bootstrap_servers, group_id, topics):
        self.consumer = Consumer({
            'bootstrap.servers': bootstrap_servers,
            'group.id': group_id,
            'auto.offset.reset': 'earliest'
        })
        self.consumer.subscribe(topics)
    
    def consume(self, callback):
        while True:
            msg = self.consumer.poll(1.0)
            if msg is None:
                continue
            if msg.error():
                print(f'Consumer error: {msg.error()}')
                continue
            
            value = json.loads(msg.value().decode('utf-8'))
            callback(value)
```

## 總結

訊息佇列是構建事件驅動架構的基礎，實現了服務間的松耦合通信。

## 相關頁面

- [微服務架構](./微服務架構.md) - 服務間通信模式

## 標籤

#訊息佇列 #Kafka #RabbitMQ #事件驅動 #異步通信
